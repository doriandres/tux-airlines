RabbitMQ

# Se van a crear estos usuarios
# rabbitadmin=sGD7RQPda$5a
# microservice=ehcwVAizE0$e

$ docker run -d --hostname rabbit-service --name rabbit-service -p 0.0.0.0:15672:15672 -p 0.0.0.0:5672:5672 -e RABBITMQ_DEFAULT_USER=rabbitadmin -e RABBITMQ_DEFAULT_PASS=sGD7RQPda$5a rabbitmq:management-alpine

$ docker exec -it rabbit-service bash
$ rabbitmqctl add_user "microservice"
$ rabbitmqctl set_user_tags microservice administrator
$ rabbitmqctl set_permissions -p / microservice "." "." "."
$ exit